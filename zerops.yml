zerops:
  - setup: svelte
    build:
      prepareCommands:
        - echo $HOME
        - curl -fsSL https://bun.sh/install | bash
      buildCommands:
        - echo $HOME
        - bun install
        - bun run build
      deploy:
        - ./build
        - package.json
    run:
      prepareCommands:
        - echo $HOME
        - curl -fsSL https://bun.sh/install | bash
      start: NODE_ENV=production bun start
